url: https://github.com/sorhawell/minipolars/
reference:
  # NSE

  - title: minipolars classes
    desc: >
      The different class of minipolars are presented here.
  - subtitle: DataFrame
    contents:
      - has_keyword("DataFrame")
  - subtitle: Expr
    contents:
      - has_keyword("Expr")
  - subtitle: everything else
    contents:
      - -has_keyword("DataFrame")
      - -has_keyword("Expr")
