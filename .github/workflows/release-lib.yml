name: Release binary libraries
on:
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/release-lib.yml
      - configure*
      - src/Makevars*
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash

env:
  LIB_NAME: libr_polars

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            target: x86_64-unknown-linux-musl
          - os: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
          - os: ubuntu-22.04-arm
            target: aarch64-unknown-linux-gnu
          - os: macos-latest
            target: x86_64-apple-darwin
          - os: macos-latest
            target: aarch64-apple-darwin
          - os: windows-latest
            target: x86_64-pc-windows-gnu
          - os: windows-11-arm
            target: aarch64-pc-windows-gnullvm
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v7

      - name: prep Rust
        working-directory: src/rust
        run: |
          LIB_VERSION="$(cargo metadata --format-version=1 --no-deps | jq --raw-output '.packages[0].version')"
          echo "LIB_VERSION=${LIB_VERSION}" >>"$GITHUB_ENV"
          rustup target add ${{ matrix.target }}
          # savvy needs R_INCLUDE_DIR envvar
          echo R_INCLUDE_DIR="$(Rscript -e 'cat(normalizePath(R.home("include")))')" >>"$GITHUB_ENV"

      - uses: rui314/setup-mold@v1

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: "src/rust -> target"
          shared-key: ${{ matrix.target }}-dist-release

      - name: prep for musl
        if: endsWith(matrix.target, '-musl')
        run: |
          sudo apt-get update
          sudo apt-get install -y musl-tools

      - name: limit rust build jobs on aarch64 linux
        if: runner.os == 'Linux' && runner.arch == 'ARM64'
        run: |
          # Serialize rustc on ARM Linux targets. With two jobs the runner runs out
          # of memory while polars-plan and polars-expr compile at the same time:
          # 16 GiB of RAM and the whole 3 GiB of swap are consumed, and the box
          # thrashes until the runner agent loses its heartbeat.
          echo 'CARGO_BUILD_JOBS=1' >>"$GITHUB_ENV"
          # Reduce LTO memory pressure on ARM Linux while keeping release optimizations.
          echo 'CARGO_PROFILE_DIST_RELEASE_LTO=thin' >>"$GITHUB_ENV"

      - name: Set up R
        uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: ${{ matrix.os != 'ubuntu-22.04-arm' }} # TODO: remove this line after r-lib/actions#1083
          Ncpus: 2

      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          packages: any::pkgbuild, deps::.
          dependencies: "'hard'"

      # Diagnostics for the aarch64 Linux build failures, where the runner itself
      # dies mid-build ("lost communication with the server" / exit code 143).
      # The public specs are identical for x86_64 and arm64 runners, so measure
      # the real machines instead of guessing.
      - name: log runner resources
        if: runner.os == 'Linux'
        run: |
          nproc
          free -h
          swapon --show || echo '(no swap configured)'
          df -h

      - name: build lib
        env:
          NOT_CRAN: "true"
          TARGET: ${{ matrix.target }}
          LIBR_POLARS_BUILD: "true"
          LIBR_POLARS_FEATURES: nightly
          LIBR_POLARS_PROFILE: dist-release
        run: |
          # Sample memory usage into this step's live log. A separate step cannot
          # report it, because when the runner is killed the remaining steps are
          # skipped even with `if: always()`.
          if command -v free >/dev/null; then
            (
              while true; do
                echo "[mem] $(date -Is) $(free -m | awk '/^Mem:/ {printf "used=%sMi avail=%sMi", $3, $7} /^Swap:/ {printf " swap=%sMi", $3}')"
                sleep 15
              done
            ) &
            MEM_SAMPLER_PID=$!
            trap 'kill "${MEM_SAMPLER_PID}" 2>/dev/null || true' EXIT
          fi

          # make sure savvy is built from source because rust-cache doesn't work well.
          (find ~/.cargo/registry/ src/rust/target -name 'savvy-*' -print0 | xargs -0 rm -rf) || true

          ARTIFACT_NAME="${LIB_NAME}-${LIB_VERSION}-${TARGET}.tar.gz"
          Rscript -e 'pkgbuild::compile_dll(debug = FALSE)'

          tar -czf "${ARTIFACT_NAME}" -C "src/rust/target/${TARGET}/${LIBR_POLARS_PROFILE}" "${LIB_NAME}.a"
          echo "ARTIFACT_NAME=${ARTIFACT_NAME}" >>"$GITHUB_ENV"

      - name: upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: libs-${{ matrix.target }}
          path: ${{ env.ARTIFACT_NAME }}

  # Build the aarch64 musl static library natively inside an Alpine (musl)
  # ARM64 container on GitHub's native ARM64 runner. This is required because
  # cross-compiling the musl target with a glibc C toolchain (the previous
  # `CC=aarch64-linux-gnu-gcc` workaround) leaked glibc fortification symbols
  # like `__fprintf_chk` into the library, which are missing on musl and broke
  # loading on Alpine (#1760). A native musl toolchain cannot leak glibc
  # symbols and avoids the jemalloc C-atomics detection failure that `musl-gcc`
  # on the Ubuntu host runner triggers.
  build-aarch64-musl:
    runs-on: ubuntu-24.04-arm
    container:
      image: alpine:3.22
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v7

      - name: install system deps
        run: |
          # R and R-dev live in Alpine's community repository
          apk add --no-cache --repository "https://dl-cdn.alpinelinux.org/alpine/v3.22/community" \
            bash curl gcc jq make musl-dev pkg-config R R-dev tar

      - name: install Rust toolchain
        run: |
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --profile minimal
          echo "$HOME/.cargo/bin" >>"$GITHUB_PATH"
          TOOLCHAIN="$(sed -n 's/^channel = "\(.*\)"/\1/p' src/rust/rust-toolchain.toml)"
          rustup toolchain install "${TOOLCHAIN}" --profile minimal
          rustup target add aarch64-unknown-linux-musl --toolchain "${TOOLCHAIN}"
          rustup default "${TOOLCHAIN}"

      - name: install R pkgbuild
        run: |
          Rscript -e 'options(repos = c(CRAN = "https://cloud.r-project.org")); install.packages("pkgbuild")'

      # Serialize rustc: the ARM runner runs out of memory while polars-plan and
      # polars-expr compile at the same time.
      - name: limit rust build jobs on aarch64 linux
        run: |
          echo 'CARGO_BUILD_JOBS=1' >>"$GITHUB_ENV"
          echo 'CARGO_PROFILE_DIST_RELEASE_LTO=thin' >>"$GITHUB_ENV"

      - name: build lib
        env:
          NOT_CRAN: "true"
          TARGET: aarch64-unknown-linux-musl
          LIBR_POLARS_BUILD: "true"
          LIBR_POLARS_FEATURES: nightly
          LIBR_POLARS_PROFILE: dist-release
        run: |
          # savvy needs R_INCLUDE_DIR at build time
          export R_INCLUDE_DIR="$(Rscript -e 'cat(normalizePath(R.home("include")))')"
          LIB_VERSION="$(cargo metadata --format-version=1 --no-deps --manifest-path src/rust/Cargo.toml | jq --raw-output '.packages[0].version')"
          echo "LIB_VERSION=${LIB_VERSION}" >>"$GITHUB_ENV"
          Rscript -e 'pkgbuild::compile_dll(debug = FALSE)'

          ARTIFACT_NAME="${LIB_NAME}-${LIB_VERSION}-${TARGET}.tar.gz"
          tar -czf "${ARTIFACT_NAME}" -C "src/rust/target/${TARGET}/${LIBR_POLARS_PROFILE}" "${LIB_NAME}.a"
          echo "ARTIFACT_NAME=${ARTIFACT_NAME}" >>"$GITHUB_ENV"

      - name: upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: libs-aarch64-unknown-linux-musl
          path: ${{ env.ARTIFACT_NAME }}

  test:
    needs: build
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - macos-latest
          - windows-latest
          - ubuntu-latest
        r:
          - "4.5"
          - release
          - devel
        exclude:
          - os: macos-latest
            r: devel
          - os: macos-latest
            r: "4.5"
        include:
          - os: macos-15-intel
            r: release
          - os: ubuntu-24.04-arm # Use 24.04, because P3M does not provide binaries for 22.04 arm64
            r: release
          - os: windows-11-arm
            r: release

    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v7

      - uses: rui314/setup-mold@v1

      - name: prep Rust
        run: |
          LIB_VERSION="$(cargo metadata --format-version=1 --no-deps --manifest-path src/rust/Cargo.toml | jq --raw-output '.packages[0].version')"
          echo "LIB_VERSION=${LIB_VERSION}" >>"$GITHUB_ENV"
          if [[ "${{ runner.os }}" == "Windows" && "${{ runner.arch }}" == "X64" ]]; then
            echo "LIB_TARGET=x86_64-pc-windows-gnu" >>"$GITHUB_ENV"
          elif [[ "${{ runner.os }}" == "Windows" && "${{ runner.arch }}" == "ARM64" ]]; then
            echo "LIB_TARGET=aarch64-pc-windows-gnullvm" >>"$GITHUB_ENV"
          elif [[ "${{ runner.os }}" == "Linux" ]]; then
            echo "LIB_TARGET=$(uname -m)-unknown-linux-gnu" >>"$GITHUB_ENV"
          else
            echo "LIB_TARGET=$(rustc -vV | grep host | cut -d' ' -f2)" >>"$GITHUB_ENV"
          fi
          rm "$(rustup which cargo)"

      - uses: actions/download-artifact@v8
        with:
          name: libs-${{ env.LIB_TARGET }}
          path: libs

      - name: prep lib
        run: |
          ARTIFACT_NAME="${LIB_NAME}-${LIB_VERSION}-${LIB_TARGET}.tar.gz"
          mkdir -p tools
          tar -xzf "libs/${ARTIFACT_NAME}" -C "tools"
          rm -rf "libs"

      - uses: r-lib/actions/setup-pandoc@v2

      - uses: r-lib/actions/setup-r@v2
        with:
          r-version: ${{ matrix.r }}
          use-public-rspm: true
          Ncpus: "2"

      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          # Only install minimum dependencies for testing
          dependencies: "'hard'"
          # TODO: move to the DESCRIPTION file
          packages: >-
            local::.,
            any::testthat,
            any::knitr,
            any::patrick,
            any::blob,
            any::hms

      - name: R test
        shell: Rscript {0}
        run: |
          testthat::test_local(load_package = "installed")

  release:
    needs:
      - build
      - build-aarch64-musl
      - test
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch'
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v7

      - uses: actions/download-artifact@v8
        with:
          pattern: libs-*
          path: libs
          merge-multiple: true

      - name: create checksums
        working-directory: libs
        run: |
          find . -type f | cut -c3- | xargs sha256sum >"../sha256sums.txt"

      - name: get version
        run: |
          new_version="$(cargo metadata --format-version=1 --no-deps --manifest-path src/rust/Cargo.toml |
            jq --raw-output '.packages[] | select(.name == "r-polars") | .version')"
          echo "TAG_NAME=lib-v${new_version}" >>"$GITHUB_ENV"

      - name: create release
        uses: ncipollo/release-action@v1.21.0
        with:
          draft: true
          immutableCreate: true
          prerelease: true
          commit: ${{ github.sha }}
          tag: ${{ env.TAG_NAME }}
          token: ${{ secrets.GITHUB_TOKEN }} # The `commit` input requires this
          artifacts: libs/*,sha256sums.txt
