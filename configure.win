#!/bin/sh

case "$(uname -m)" in
    "x86_64") default_target="x86_64-pc-windows-gnu" ;;
    "aarch64") default_target="aarch64-pc-windows-gnullvm" ;;
    *) default_target="" ;;
esac
export TARGET="${TARGET:-$default_target}"

. ./configure
