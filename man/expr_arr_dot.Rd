% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expr-array.R
\name{expr_arr_dot}
\alias{expr_arr_dot}
\title{Compute the row-wise dot product with another Array expression}
\usage{
expr_arr_dot(other)
}
\arguments{
\item{other}{Array expression or query vector to compute the dot product
with. A one-row input is broadcast against the other input.}
}
\value{
A polars \link{expression}
}
\description{
Both inputs must contain arrays of the same width. Their inner data types
are cast to a common supertype, which must be \code{Float32} or \code{Float64}. An
input with one row is broadcast against the other input. If either input
array is \code{null} for a row, the result for that row is \code{null}.
}
\details{
Elements are paired by position. Pairs where either element is \code{null} do not
contribute to the sum. If a non-null row has no pairs where both elements
are valid, the result is \code{0}.
}
\examples{
df <- pl$DataFrame(
  a = list(c(1, 2), c(3, 4)),
  b = list(c(5, 6), c(7, 8))
)$cast(pl$Array(pl$Float64, 2))

df$with_columns(dot = pl$col("a")$arr$dot("b"))
}
