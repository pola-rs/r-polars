% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataframe__frame.R
\name{DataFrame_with_row_index}
\alias{DataFrame_with_row_index}
\title{Add a column for row indices}
\usage{
DataFrame_with_row_index(name, offset = NULL)
}
\arguments{
\item{name}{string name of the created column}

\item{offset}{positive integer offset for the start of the counter}
}
\value{
A new \code{DataFrame} object with a counter column in front
}
\description{
Add a new column at index 0 that counts the rows
}
\examples{
df = pl$DataFrame(mtcars)

# by default, the index starts at 0 (to mimic the behavior of Python Polars)
df$with_row_index("idx")

# but in R, we use a 1-index
df$with_row_index("idx", offset = 1)
}
\keyword{DataFrame}
